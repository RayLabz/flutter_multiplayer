name: multiplayer
description: A Flutter package for implementing multiplayer game features.
version: 0.0.0
homepage: "https://pub.dev/packages/multiplayer/"
repository: "https://github.com/RayLabz/flutter_multiplayer"

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
