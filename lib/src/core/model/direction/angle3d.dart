/// A class representing a 3D angle with four components: x, y, z, and w.
class Angle3D {

  final double x;
  final double y;
  final double z;
  final double w;

  const Angle3D(this.x, this.y, this.z, this.w);

}